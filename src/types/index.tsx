export interface ISale {
    _id?: string;
    name: string;
    sale: string;
    usageLimit: number;
    expirationDate: string;
    createdAt: string;
}
export interface IPayment {
    _id?: string;
   
    createdAt: string;
}
